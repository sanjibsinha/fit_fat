import 'package:flutter/material.dart';

class FitFatBlankPage extends StatelessWidget {
  const FitFatBlankPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
